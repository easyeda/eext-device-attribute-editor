# 1.8.1

## 修复

1. 变更扩展名为正确的扩展名
2. 增加兼容性标签
3. 禁止删除“器件”之外的属性绑定信息放置误操作


# 1.7.1

## 新增

1. 属性编辑表格占满可用空间，不再有固定最大高度限制
2. 搜索过程实时显示已搜索器件数量（逐器件更新进度）
3. 达到搜索上限（10 万器件）时给出明确提示

## 修正

1. 暗色主题全面适配：按钮、边框、表格表头、提示文字等对比度修复
2. 暗色主题 `data-theme` 属性值修正（`toggleAttribute` → `setAttribute`），暗色模式现已正常生效
3. 点击器件后纵向滚动失效问题（flex 嵌套缺少 `min-height:0`）
4. 搜索结果全选复选框状态未在重新搜索时重置
5. 批量操作进度条始终不可见（CSS `display:none` 与 inline style 冲突）
6. 编辑器属性高亮定位使用中文搜索时失败（CN_MAP 映射未反向查找）

## 优化

1. 搜索性能优化：`innerHTML` → `createElement`+`textContent`，减少 HTML 解析
2. 搜索结果滚动节流（每 5 条匹配更新一次），减少强制 reflow
3. HTML/CSS/JS 三文件分离，提升可维护性
4. 确认弹窗文案优化：显示具体属性名

## 变更

1. 搜索 API 参数调整：`pageSize` 50→100，`maxPages` 10→1000
2. 批量操作进度条移至列表底部

# 1.0.0

初始版本
